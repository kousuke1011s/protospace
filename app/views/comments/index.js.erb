$("textarea").val('');
$("#comments").html("<%= j(render partial: 'comments/index', locals: { comments: @comment.prototype.comments.order(id: "DESC") })%>");
$(".comment-number").html("<%= "Comments(#{@comment.prototype.comments.length})"%>");
